[Unit]
Description="dnsmasq for consul"
Wants=network-online.target
After=network-online.target

[Service]
ExecStart=/usr/local/sbin/dnsmasq --conf-dir {{ consul_dnsmasq_conf_dir }} -d --log-queries
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target